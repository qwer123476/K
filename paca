local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "키보드 핵",
   LoadingTitle = "Paca Hub",
   LoadingSubtitle = "paca hub loading..",
   ConfigurationSaving = { Enabled = false },
   Discord = { Enabled = false },
   KeySystem = false
})

local StageTab = Window:CreateTab("스테이지 선택", 4483345998)
local FarmTab = Window:CreateTab("오토팜", 4483345998)
local TreadmillTab = Window:CreateTab("러닝머신 오토팜", 4483345998)
local playerTab = Window:CreateTab("플레이어설정", 44833646998)

local StageCoordinates = {
    [1]  = Vector3.new(-17, 13, 287),
    [2]  = Vector3.new(-18, 14, 507),
    [3]  = Vector3.new(-18, 82, 774),
    [4]  = Vector3.new(-15, 82, 1108),
    [5]  = Vector3.new(-15, 83, 1409),
    [6]  = Vector3.new(-539, 61, 1451),
    [7]  = Vector3.new(-1009, 59, 1448),
    [8]  = Vector3.new(-1121, 300, 1447),
    [9]  = Vector3.new(-2972, 300, 1447),
    [10] = Vector3.new(-3941, 304, 1448),
    [11] = Vector3.new(-4368, 477, 1511),
    [12] = Vector3.new(-5342, 479, 1456),
    [13] = Vector3.new(-6808, 525, 1467),
    [14] = Vector3.new(-8354, 490, 1470),
    [15] = Vector3.new(-8352, 490, 1469),
    [16] = Vector3.new(-14002, 759, 3066)
}

local SelectedStage = 1
local FarmDelay = 6
local AutoFarmEnabled = false
local TreadmillEnabled = false
local AntiAfkEnabled = false
local AntiAfkConnection = nil

local plr = game.Players.LocalPlayer
local walkSpeedT = false
local walkSpeedV = 80
local jumpPowerT = false
local jumpPowerV = 60

local noClipT = false
local noClipConnection = nil

local FLYING = false
local iyflyspeed = 1
local flyKeyDownConnection, flyKeyUpConnection = nil, nil
local BG, BV = nil, nil

local RunService = game:GetService("RunService")
local VirtualUser = game:GetService("VirtualUser")
local UserInputService = game:GetService("UserInputService")

local function teleportToStage(stageNum)
    if plr and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
        local targetPos = StageCoordinates[stageNum]
        if targetPos then
            plr.Character.HumanoidRootPart.CFrame = CFrame.new(targetPos)
        end
    end
end

function updateWalkSpeedF()
    local function apply(char)
        local hum = char:WaitForChild("Humanoid")
        if walkSpeedT then
            hum.WalkSpeed = walkSpeedV
            hum:GetPropertyChangedSignal("WalkSpeed"):Connect(function()
                if walkSpeedT then hum.WalkSpeed = walkSpeedV end
            end)
        else
            hum.WalkSpeed = 16
        end
    end
    if plr.Character then apply(plr.Character) end
    plr.CharacterAdded:Connect(apply)
end

function updateJumpPowerF()
    local function apply(char)
        local hum = char:WaitForChild("Humanoid")
        if jumpPowerT then
            hum.JumpPower = jumpPowerV
            hum:GetPropertyChangedSignal("JumpPower"):Connect(function()
                if jumpPowerT then hum.JumpPower = jumpPowerV end
            end)
        else
            hum.JumpPower = 26
        end
    end
    if plr.Character then apply(plr.Character) end
    plr.CharacterAdded:Connect(apply)
end

function updateNoClipF()
    if noClipConnection then
        noClipConnection:Disconnect()
        noClipConnection = nil
    end
    if not noClipT then return end

    noClipConnection = RunService.Stepped:Connect(function()
        if not noClipT or not plr.Character then
            if noClipConnection then
                noClipConnection:Disconnect()
                noClipConnection = nil
            end
            return
        end
        for _, child in pairs(plr.Character:GetDescendants()) do
            if child:IsA("BasePart") and child.CanCollide == true then
                child.CanCollide = false
            end
        end
    end)
end

function sFLY()
    FLYING = true
    local char = plr.Character
    if not char then return end
    local humanoid = char:FindFirstChildOfClass("Humanoid")
    local T = char:FindFirstChild("HumanoidRootPart") or char:FindFirstChild("Torso") or char:FindFirstChild("UpperTorso")
    if not T then return end

    if humanoid then humanoid.PlatformStand = true end

    BG = Instance.new('BodyGyro')
    BV = Instance.new('BodyVelocity')
    BG.P = 9e4
    BG.Parent = T
    BV.Parent = T
    BG.MaxTorque = Vector3.new(9e9, 9e9, 9e9)
    BG.CFrame = T.CFrame
    BV.Velocity = Vector3.new(0, 0, 0)
    BV.MaxForce = Vector3.new(9e9, 9e9, 9e9)

    local CONTROL = {F = 0, B = 0, L = 0, R = 0, Q = 0, E = 0}
    
    flyKeyDownConnection = UserInputService.InputBegan:Connect(function(input, gpe)
        if gpe then return end
        if input.KeyCode == Enum.KeyCode.W then CONTROL.F = 1
        elseif input.KeyCode == Enum.KeyCode.S then CONTROL.B = -1
        elseif input.KeyCode == Enum.KeyCode.A then CONTROL.L = -1
        elseif input.KeyCode == Enum.KeyCode.D then CONTROL.R = 1
        elseif input.KeyCode == Enum.KeyCode.E then CONTROL.Q = 1
        elseif input.KeyCode == Enum.KeyCode.Q then CONTROL.E = -1
        end
    end)

    flyKeyUpConnection = UserInputService.InputEnded:Connect(function(input, gpe)
        if input.KeyCode == Enum.KeyCode.W then CONTROL.F = 0
        elseif input.KeyCode == Enum.KeyCode.S then CONTROL.B = 0
        elseif input.KeyCode == Enum.KeyCode.A then CONTROL.L = 0
        elseif input.KeyCode == Enum.KeyCode.D then CONTROL.R = 0
        elseif input.KeyCode == Enum.KeyCode.E then CONTROL.Q = 0
        elseif input.KeyCode == Enum.KeyCode.Q then CONTROL.E = 0
        end
    end)

    task.spawn(function()
        local camera = workspace.CurrentCamera
        while FLYING and T and T.Parent do
            if humanoid then humanoid.PlatformStand = true end
            BG.CFrame = camera.CFrame
            
            local dir = Vector3.new()
            if CONTROL.F + CONTROL.B ~= 0 or CONTROL.L + CONTROL.R ~= 0 or CONTROL.Q + CONTROL.E ~= 0 then
                dir = ((camera.CFrame.LookVector * (CONTROL.F + CONTROL.B)) + (camera.CFrame.RightVector * (CONTROL.L + CONTROL.R)) + (Vector3.new(0,1,0) * (CONTROL.Q + CONTROL.E)))
            end
            
            BV.Velocity = dir * (iyflyspeed * 50)
            task.wait()
        end
    end)
end

function NOFLY()
    FLYING = false
    if flyKeyDownConnection then flyKeyDownConnection:Disconnect() flyKeyDownConnection = nil end
    if flyKeyUpConnection then flyKeyUpConnection:Disconnect() flyKeyUpConnection = nil end
    if BG then BG:Destroy() BG = nil end
    if BV then BV:Destroy() BV = nil end
    if plr.Character then
        local humanoid = plr.Character:FindFirstChildOfClass("Humanoid")
        if humanoid then humanoid.PlatformStand = false end
    end
end

function updateAntiAfkF()
    if AntiAfkConnection then
        AntiAfkConnection:Disconnect()
        AntiAfkConnection = nil
    end
    if AntiAfkEnabled then
        AntiAfkConnection = plr.Idled:Connect(function()
            VirtualUser:Button2Down(Vector2.new(0, 0), game.Workspace.CurrentCamera.CFrame)
            task.wait(1)
            VirtualUser:Button2Up(Vector2.new(0, 0), game.Workspace.CurrentCamera.CFrame)
        end)
    end
end
StageTab:CreateLabel("이동할 스테이지 선택하세요")
StageTab:CreateLabel("자신의 맞는 레벨만 보상들어옵니다.")


for i = 1, #StageCoordinates do
    StageTab:CreateButton({
        Name = "스테이지 " .. i,
        Callback = function()
            SelectedStage = i
            Rayfield:Notify({
                Title = "선택 완료",
                Content = "스테이지 " .. i .. " 선택 완료! 오토팜 칸으로 가주세요.",
                Duration = 4,
                Image = 4483345998,
            })
        end,
    })
end

FarmTab:CreateButton({
    Name = "선택한 스테이지로 즉시 이동",
    Callback = function()
        teleportToStage(SelectedStage)
    end,
})

FarmTab:CreateLabel("⚠️ 한번만 이동합니다. 오토팜은 밑의 토글입니다.")

FarmTab:CreateSlider({
    Name = "오토팜 주기 설정",
    Range = {1, 60},
    Increment = 1,
    Suffix = "초",
    CurrentValue = 6,
    Flag = "FarmDelaySlider",
    Callback = function(Value)
        FarmDelay = Value
    end
})

FarmTab:CreateToggle({
    Name = "오토팜 시작/정지",
    CurrentValue = false,
    Flag = "AutoFarmToggle",
    Callback = function(Value)
        AutoFarmEnabled = Value
        if AutoFarmEnabled then
            task.spawn(function()
                while AutoFarmEnabled do
                    teleportToStage(SelectedStage)
                    task.wait(FarmDelay)
                end
            end)
        end
    end,
})

TreadmillTab:CreateToggle({
    Name = "러닝머신 오토팜 시작/정지",
    CurrentValue = false,
    Flag = "TreadmillToggle",
    Callback = function(Value)
        TreadmillEnabled = Value
        if TreadmillEnabled then
            task.spawn(function()
                while TreadmillEnabled do
                    if plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
                        plr.Character.HumanoidRootPart.CFrame = CFrame.new(18.552, 10.831, -41.459)
                        plr.Character.HumanoidRootPart.Anchored = true
                    end
                    task.wait(0.5)
                end
            end)
        else
            if plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
                plr.Character.HumanoidRootPart.Anchored = false
            end
        end
    end
})

TreadmillTab:CreateToggle({
    Name = "안티 AFK 튕김방지",
    CurrentValue = false,
    Flag = "AntiAfkToggle",
    Callback = function(Value)
        AntiAfkEnabled = Value
        updateAntiAfkF()
    end
})

playerTab:CreateToggle({
    Name = "이동 속도",
    CurrentValue = false,
    Flag = "28",
    Callback = function(Value)
        walkSpeedT = Value 
        updateWalkSpeedF()
    end
})

playerTab:CreateInput({
    Name = "값",
    CurrentValue = 80,
    PlaceholderText = "Value",
    RemoveTextAfterFocusLost = true,
    Flag = "29",
    Callback = function(Value)
        walkSpeedV = tonumber(Value) or 80
        updateWalkSpeedF()
    end
})

playerTab:CreateToggle({
    Name = "점프 높이",
    CurrentValue = false,
    Flag = "30",
    Callback = function(Value)
        jumpPowerT = Value
        updateJumpPowerF()
    end
})

playerTab:CreateInput({
    Name = "값",
    CurrentValue = 60,
    PlaceholderText = "Value",
    RemoveTextAfterFocusLost = false,
    Flag = "31",
    Callback = function(Value)
        jumpPowerV = tonumber(Value) or 60
        updateJumpPowerF()
    end
})

playerTab:CreateSection("나머지")

playerTab:CreateToggle({
    Name = "노클립",
    CurrentValue = false,
    Flag = "32",
    Callback = function(Value)
        noClipT = Value
        updateNoClipF()
    end
})

